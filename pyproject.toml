[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "wapp-planner"
version = "0.1.0"
description = "WhatsApp Service Planner Automation — ingest WhatsApp job requests, classify intent, generate quotes/execution plans via LLM, deliver with human-in-the-loop approval."
requires-python = ">=3.12"
readme = "README.md"
authors = [{ name = "rcastro", email = "rcastro@xchangealpha.com" }]
dependencies = [
    "pydantic>=2,<3",
    "pydantic-settings>=2,<3",
    "httpx>=0.27",
    "jinja2>=3",
    "python-docx>=1.1",
    "reportlab>=4",
    "fastapi>=0.111",
    "uvicorn[standard]>=0.30",
    "openai>=1.40",
    "boto3>=1.34",
]

[project.optional-dependencies]
dev = ["pytest>=8", "pytest-cov>=5", "pytest-asyncio>=0.23", "ruff>=0.5", "mypy>=1.10"]

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
addopts = "-ra"
asyncio_mode = "auto"

[tool.coverage.run]
branch = true
source = ["wapp_planner"]

[tool.coverage.report]
# TST-01: 100% line + branch coverage of application code.
fail_under = 100
show_missing = true
exclude_lines = [
    "pragma: no cover",
    "if TYPE_CHECKING:",
    "raise NotImplementedError",
    "@(abc\\.)?abstractmethod",
]

[tool.ruff]
line-length = 100
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "C4", "SIM"]

[tool.mypy]
python_version = "3.12"
strict = true
packages = ["wapp_planner"]
